[project]
name = "cursor-patch-intent-twin"
version = "0.0.1"
description = "Scaffold: Patch Intent Twin (independent Cursor / Anysphere lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
