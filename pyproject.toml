[project]
name = "cursor-patch-intent-twin"
version = "1.0.0"
description = "Unified-diff intent comparison and merge-blocking patch receipts"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
